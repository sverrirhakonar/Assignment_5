def test_math_addition():
    # This is the *test function name*. Pytest will auto-discover it
    # because it starts with "test_".
    #
    # The body of the test uses a plain Python "assert".
    # If the condition is True, the test passes.
    # If the condition is False, the test fails.

    assert 1 + 1 == 2
